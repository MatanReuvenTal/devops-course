apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
data:
  APP_VERSION: "1.2.3"
  ENVIRONMENT: "production"
  nginx.conf: |
    server {
      listen 80;
      location / {
          root /usr/share/nginx/html;
          index index.html;
      }
    }